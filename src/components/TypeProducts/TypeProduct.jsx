import React from 'react';

const TypeProduct = ({ name }) => {
  return (
    <div style={{ display: 'flex', padding: '0 10px', fontSize: '16px', alignItems: 'center' }}>
      {name}
    </div>
  );
};

export default TypeProduct;
